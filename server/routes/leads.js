const express = require('express')
const router = express.Router()

// Placeholder — implemented in Milestone 2
router.get('/', (req, res) => res.json({ leads: [], total: 0 }))

module.exports = router
